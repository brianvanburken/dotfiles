# vim: set ft=zsh:
#!/usr/bin/env zsh
case "${OSTYPE}" in
  linux*) echo "$(upower -i $(upower -e | grep BAT) | grep 'percentage' | cut -f2 -d':' | xargs)" ;;
  darwin*) echo "$(pmset -g batt | egrep "([0-9]+\%).*" -o --colour=auto | cut -f1 -d';' | xargs)" ;;
esac
