o () { open ${1:-.}; }

gbd () { git branch -D `git branch | grep ${1}` }

brew_clean() {
  brew doctor;
  brew prune;
  brew cleanup --force;
  brew cask cleanup;
  rm -rf "$(brew --cache)/*";
}

mcdir () {
  mkdir -p "$1"
  cd "$1"
}
