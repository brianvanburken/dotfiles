function o() {
    open ${1:-.};
}

function brew_clean() {
    brew doctor;
    brew cleanup;
    rm -rf "$(brew --cache)/*";
    rm -rf ~/Library/Caches/Homebrew;
}

function mcdir() {
    mkdir -p "$1";
    cd "$1";
}
